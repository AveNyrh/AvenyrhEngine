/**
 * Class that contains all game constants 
 **/
class Const 
{
    public static var FPS (default, null) = 60;

    public static var GRID (default, null) = 32;

    public static var UNIQ (default, null) = 0;

    public static var SCALE (default, null) = 1;

    //Layers
    public static var LAYER_BG (default, null) = 0;
    public static var LAYER_BG_FX (default, null) = 1;
    public static var LAYER_MAIN (default, null) = 2;
    public static var LAYER_FRONT (default, null) = 3;
    public static var LAYER_FRONT_FX (default, null) = 4;
    public static var LAYER_IG_UI (default, null) = 10;
}